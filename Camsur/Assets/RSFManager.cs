using Photon.Pun;
using Photon.Realtime;

public class RSFManager : MonoBehaviourPunCallbacks
{
    public string levelName;

    // Start is called before the first frame update
    void Start()
    {
    }
    // Update is called once per frame
    void Update()
    {
        
    }
}
